receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318

exporters:
  debug:
    verbosity: normal
  otlp/elastic:
    endpoint: "http://localhost:4317" # Elastic APM Server 으로 수정해야함.

service:
  pipelines:
    traces:
      receivers: [otlp]
      exporters: [debug]
